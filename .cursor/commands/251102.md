when sorting by long,
algo is working correctly from tract 1 - 31 (04005002000):
assuming the algo checked for and found the tract that hit the state northern boundary (tract #28 04015950101), 
then the algo correctly selected the east-clockwise tract #29 04015950103, then selected #30 04015950102 because it is enclosed withing the previous tract 04015950103, 

but it may be that the algo was just following the west-clockwise adjacent check and not detecting the state northern boundary which should have triggered to zigzag south by selecting south-clockwise adjacent tract until state southern border tract is selected triggering a switch to zigzag back north, etc.

# next prompt

last change was undone becaseu while it helps with our current state test case, it will break if the census tracts hit a peninsula or other geographic barrier, in which case the clockwise adjacent algo should trace around such a landmark. instead i recommend to precalculate all state boundary tracts. an approach for determining is a tract is along the state border would be to merge all census tract polygons into one polygon, the use that polygon to find tracts that have the same border as the merged polygon. if you have a better approach use it.

# prompt 12:32

looks like we need to change how moving south should detect next selected tract. currently a south-clockwise adjacent check is used. but this break in geographical layouts where sorting should instead select the adjactent northwestern-clockwise tract, if no adjacent then select the most unselected northwestern-clockwise tract (biased towards north).