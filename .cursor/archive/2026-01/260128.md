# 260128 HBJB

## 2029
see @console.debug/localhost-1769660882546.log

explain line 6

## 2039 Plan

### Defining the geodisticts protocol
- detail specification 
 - data model
 - data sources
 - algorithm 
   - geodistrict boundary calculation/generation
   - calculating demographics for new geodistricts (e.g. political party, gender, race, age, etc)
   - comparision calculations/metrics (existing districts vs geodistricts)
- Generative AI 
 - the protocol can be provided as context to an LLM code generative model which produce geodistricts boundaries for all US 50 states and all other protocol specifications such as UI/UX visualization design (e.g. UI should allow user to visualize the map at each stage of the geodistricts algorithm division step and related caching design to support client performance as user navigates the steps)
### define to create protocol on github 
- protocol improvement process
 - instuctions on the governance of maintaining the protocol
  - open source project best practices following PIP models of other web3 projects, e.g. bitcoin, ethereum, solana, nostr, bluesky, etc.
- documentation for protocol implementers, e.g. state governments, organizations (e.g. re-districting consultants), developers, etc.
 - considerations for government research/exploritory and adoption/codification

## 2124 Plan cont.
changes to generated plan
### section 1.1  
- using other web3 projects as model (e.g. nostr and NIPs, or Ethereum and EIP,ERC) section 1.1 should not be one monolithic protocol spec. instead the spec should be organized such that improvements and features are either required or optional, can be maintained atomically, etc.
 - see https://nips.nostr.com/ https://eips.ethereum.org/ 

### section 1.2
- gen ai to create reference implementations
- how to document details on prompting, architecture, deployment, maintaining/improving, etc. 
 - e.g. using this project as a reference implemention of the protocol, what model, IDE/CLI, in addition to all the exisint code, documentation, prompt archive/history, etc. and any changes needed to be consumed in the context of the GDPIP (GeoDistricts Protocol Improvement Proposols)

## 2151 Plan cont.
- building/executing this plan is expected be iterative. that is, each GDPIP will need review and revision, edits, etc. 
- considering that this project (geodistricts) is not the protocol repo, we need to create a separate github project, add to the plan recommendations for creating the new github repo, e.g. should it be a separate org, or is https://github.com/Lacoda-Labs ok? consider that lacoda-labs org will have other open source projects (e.g. web3 democracy/NDN protocal, alivevote reference implementation, etc.)
