# Sat Nov 22, 2025 vibe engineering the geodistricts protocol/algorithm


## goal: to debug the process of moving isolated tracts in order to create contiguous and ration balances districts when dividing a district group (DG).

@see .cursor/commands/251122.md

### 13:33
- change algorithm to separate out the isolation logic.
- instead of having the algorithm call the isolation logic after sort and divide, add UI button controls to the .info-header (maps page). 
  - start with just a button to execute the isolated tract detection of the newly divided tracts (north/south or east/west) from the DG being divided. change the color of any isolation tracts to a shade darker than the color of the rest of the DG.

### 15:21
- isolated tracts are found by backend (see logs), but frontend is not showing the tracts (not changing color)

### 15:38
- after the response from clicking the detect isolation button, list the isolated tracts in a table. put list in the step-info-container before step-header.
- if isolation tracts are found, show a button for detect bridge tracts, then handle response by doubling the border width of the bridge tracts and add to same isolation list next to tracts it is bridging. 
- hide the district-groups-container for now. more changes to this later.

### 16:40
- in the isolation tract list, when user clicks on tract row, show info bubble same as when user clicks on tract in map
- for highlighted bridge tracts, change the tract border color white and make 3px width.

### 16:57
- any isolated tracts that are enclosed display a material Icon <mat-icon fontIcon="explicit"> next to the tract id in the list. 

### 17:29
- isolation detection after step 3 is identifying the primary tract component as isolated. it has the most reachable tracts.

### 18:06
- bridge detection appears to be failing for DG 4-5. The 97 isolated tract(s) detected in DG 4 are separated by District 5 Tract ID: 010102. 

### 18:55
- isolation and bridge detection appears to be working now. add a button to move bridge tracts to the other DG, then run isolation detection again and return result.

### 19:11
- add a button to move isolated tracts to opposite group.

### 2008
- to move an isolated tracts means to swap its DG with the opposite DG from the parent DG. e.g. DG 4-5 divides into DG 4 and DG 5. if an isolated tract is in DG 4, then it should moved/swapped to DG 5. it is expected that that will join isolated tracts into the opposite group eliminting all isolated and making the max reachable equal to the total tracts in the DG. have I missed something? 
is this your approach?

### 2114
- save the DG parent/sibling relationships in the step metadata so that it can be quickly read (not calculated). is this the approach taken?

### 2154
- move bridge tracts should follow same sibling move logic.

### 2201
- for AZ after 1st division, there are two tracts that correctly are assigned to District 6-9: Tract ID: 002105 and 002104. however, after next division step they both belong to District 4-5. how does this happen? they should be in District 8-9.

### 2207
- there has been an issue with duplicate tracts. this should never happen if when census tract data is loaded (include polygon and s4 adjacency). if a canonicle tract model that used a key based collection (e.g. set or map) then duplicate tracts would never happen. further, it is expected that census tract data comes from census api, and that should be used as the primary record for a tract, then TIGER polygon data is related via the tract id which should be the format from census api. brown-s4 data should also be child to the parent census tract. this way there should never be any duplicate tract data.

### 2236
- when detect isolation is clicked and no isolation tracts are detected, display a message that says no isolation tracts found and "max reachable tracts" and "DG total tracts" values.

### 2044
- see ### 2201
- this happens when the "Move isolated tracts" button is clicked.  

### 2048
you just made changes to implement a canonical tract model (2207). there should be no duplicates. how are the new models represented in the cache? do changes need to be made?

### 2317
persisting the tract division into two siblings relationships is critical to moving isolated tracts, since a move just means changing tract to sibling tract. find out why this is not happening. 2201 is still a problem, when moving isolated tracts, these two are still moving to 4-5. why?

### 2327
a tract in each step updates its properties for tract_DG=DG6-7, parent_DG=DG6-9, and sibling_DG=DG8-9. then when a tract is isolated and instructed to swap, it swaps values between tract_DG and sibling_DG.

### 2345
moving tracts is working now for previous steps. on final step 4, moving isolated should just swap all 105 tracts with their siblings. instead clicking move just move on tract. why?