# 251101 vibe session to work on the /geodistrict page 

tonight we are working on fixing up the geodistrict page so that we don't need the tract-debug page. much of the tract-debug page does is provided on the geodistrict page.
let's fill the gap on the geodistrict page with debug features on tract-debug so that we can delete that page.

## make the following changes to the geodistrict page:

1. make state default AZ. make algorith default geo-graph
2. big layout change for .district-groups:
- the grid should represent the division iteration (step) in the algorithm
e.g. 
Step 1: Initial state: All tracts in single group 
// shows entire state in one district group. but in next step this district group is divided into to rows. therefore this first district group should be flex-direction: column
```html
<div class="district-group longitude iteration0">
</div>
```
```css
.district-group.longitude {
    flex-direction: column;
}
.district-group.latitude {
    flex-direction: row;
```

Step 2. First division by latitude
//since we are dividing into rows (by lat), make the .district-group layout as row.
```html
<div class="district-group longitude iteration0">
    <div class="district-group latitude iteration1">
    </div>
</div>
```
Step 3. Division 2 by longitude
```html
<div class="district-group longitude iteration0">
    <div class="district-group latitude iteration1">
        <div class="district-group longitude iteration2">
        </div>
    </div>
</div>
```
```
the result is a nest grid dividing each section (div) with each subsequent iteration to represent the dividing of a state into district groups. note that classes iteration0, iteration1, etc. represent steps/iterations of dividing district groups in the algorith, so this classes need to be dynamically generated.

once changes 1. and 2. pass UAT (me testing in the UI) proceed to change 3. below. I will let you know :)

3. map update to visually isolate district group. the goal is to remove the visual noise and focus on the division process and resulting district groups.
- add layer with white background that hides the default map layer showing the states, etc.; position white layer between default layer and geodistrict layers.
- add checkbox that allows user to show/hide the census tract polygon border lines that are inside the district group (assuming a district group is drawn by drawing each census tract polygon). i.e. perhaps merge all tract polygons to create on district group polygon (depending on checkbox state)
