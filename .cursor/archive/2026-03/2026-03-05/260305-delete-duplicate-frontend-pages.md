# 260305

## 1400 delete duplicate frontend pages

### Prompt

@pages can this be deleted

### Response / actions

Confirmed that `frontend/frontend/` was a duplicate of the maps-page component (HTML, TS, SCSS) and that the real app lives under `frontend/src/app/pages/`. ✅ **RESOLVED**: Deleted the three duplicate files: `frontend/frontend/src/app/pages/maps-page.component.html`, `maps-page.component.ts`, and `maps-page.component.scss`. Suggested removing any remaining empty `frontend/frontend` dirs if desired.
