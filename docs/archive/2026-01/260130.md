# 260130

## 2300

algorithm

- check if code base is using just one algorithm, i.e. all other algorithms and support for multiple algorithms has been removed. remove if needed so that only the one lat/long division algorithm is used.

- documentation mentions county being used for division algorithm, but this seems unnecessary considering the algorithm. does the aglorithm use county in the implementation?