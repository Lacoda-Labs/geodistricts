

### 01:04
- detect bridge tracts seems to work, but moving bridge tracts is swapping with tracts from other parent DG, instead swapping with sibling. is the cause logic or caching or something else?

**FIXED:**
1. **Frontend logic issue**: The `moveBridgeTracts()` method was collecting ALL bridge tracts from ALL isolated groups and moving them all to the FIRST isolated group's sibling. Fixed to process each isolated group separately - each group's bridge tracts now move to that group's own sibling.

2. **Caching issue**: The `siblingGroups` property in `divisionLines` wasn't being preserved when normalizing step data for caching. This caused `findSiblingGroup()` to fall back to calculated method which could find wrong siblings. Fixed by adding `siblingGroups` and `step` to the normalization in `normalizeStepData()`.

### 1:11
- move isolated tracts not working. 

**FIXED:**
1. **Unreliable calculated sibling method**: `findSiblingGroup()` had a fallback that calculated siblings based on adjacent district numbers (e.g., finding 4-5 as sibling of 6-7 because 6-1=5). This was unreliable and could find wrong siblings. Removed the calculated fallback - now only uses divisionLines metadata. If no sibling found in metadata, returns null to trigger neighbor-based fallback.

2. **Better sibling lookup**: Improved `findSiblingGroup()` to properly sort divisionLines by step number (most recent first) to find the correct sibling from the immediate parent division.

3. **Neighbor-based fallback**: When sibling can't be found from metadata, the code now uses a neighbor-based approach that finds the group where isolated tracts have the most neighbors, which is more reliable than the calculated method.

### 1:15
- there should not be a findSiblingGroup() function because a siblings are always 
just the two DGs. therefore after sorting and division, each tract is assigned its siblingDG which is the other half DG from dividing the parentDG. e.g. DG6-7 is divided into DG6-6/DG7-7, so DG6-6 has siblingDG=DG7-7. and DG7-7.siblingDG=DG6-6.

**FIXED:**
1. **Removed `findSiblingGroup()` function**: As the user correctly pointed out, siblings are always just the two DGs from a division. Every tract should have `sibling_DG` set in its properties after division. Removed the `findSiblingGroup()` function entirely.

2. **Use `sibling_DG` directly from tract properties**: Both `moveIsolatedTractsToOppositeGroup()` and `moveBridgeTractsAndRecheck()` now use `sibling_DG` directly from tract properties. If `sibling_DG` is missing (data integrity issue), the code attempts to set it from `divisionLines` metadata and then uses it.

3. **Data integrity fix**: If `sibling_DG` is missing from tract properties, the code now fixes it by setting it from `divisionLines` metadata before using it, ensuring all tracts have the correct `sibling_DG` set.

4. **Simplified logic**: Removed all fallback logic - if `sibling_DG` can't be found or set, the code throws an error instead of using unreliable fallbacks. This ensures data integrity and makes bugs more obvious.

### 1:31
- step 3 of 4: 
Tract ID: 010102 is in DG4-5
- step 4 of 4:
Tract ID: 010102 is in DG5 and correctly identified as a bridge tract.
clicking move bridge assigns DG5 to DG1-2 instead of DG4.
check that sibling_DG is being used and is correctly assigned during step division to DG4.

**FIXED:**
1. **Bridge tract detection excluding sibling group**: Bridge tract detection was finding tracts from ALL groups except the isolated group, including tracts already in the sibling group (the target group). Fixed to exclude the sibling group from bridge tract candidates - bridge tracts should only come from OTHER groups, not from the sibling group where they're being moved to.

2. **Debug logging for sibling_DG assignment**: Added debug logging during division to verify that `sibling_DG` is correctly assigned to tracts, especially for tract 010102 mentioned in the issue.

3. **Debug logging for bridge tract detection**: Added debug logging to show tract properties (tract_DG, sibling_DG, parent_DG) when detecting bridge tracts, to help verify that sibling_DG is being used correctly.

### 1:43
- bridge tracts detection and movement should always be just between siblings (after a DG division).

**FIXED:**
1. **Bridge tract detection only from sibling group**: Changed bridge tract detection to ONLY look for bridge tracts in the sibling group (the other half from the same parent division). Previously, it was looking in all groups except the isolated group and sibling group, but bridge tracts should only come from the sibling group since they're the result of a division between two sibling groups.

2. **Simplified detection logic**: Since bridge tracts only come from the sibling group, the detection logic is now simpler - it only checks neighbors of isolated tracts that are in the sibling group, not in any other groups.

3. **Skip if no sibling found**: If the sibling group cannot be found for an isolated group, bridge tract detection is skipped for that group (with a warning), since bridge tracts can only exist between siblings.

### 1:52
- make sure when moving bridge tracts, they are just swapped with sibling_DG.

**FIXED:**
1. **Swap tract_DG with sibling_DG when moving bridge tracts**: Added the same swap logic used for isolated tracts to bridge tract movement. When a bridge tract is moved, it now swaps `tract_DG` with `sibling_DG` (tract_DG becomes old sibling_DG, sibling_DG becomes old tract_DG), ensuring the tract properties correctly reflect its new group assignment.

2. **Verification and logging**: Added verification to ensure the new `tract_DG` matches the target group after the swap, and added logging to show the swap operation for debugging.