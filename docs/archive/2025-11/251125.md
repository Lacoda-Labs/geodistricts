continued from .cursor/commands/251124.md

## 2127

- step 4 of 4 does not move isolated tracts.

**FIXED:**
1. **Final step data structure mismatch**: When the algorithm completes (`isComplete: true`), the backend returns a `result` object with `finalDistricts` instead of a `step` object with `districtGroups`. The frontend was trying to use `newStep.districtGroups` which doesn't exist in the result object, causing `moveIsolatedTracts()` to fail because `this.currentStep.districtGroups` was undefined.

2. **Convert result to step object**: Fixed by detecting when `isComplete: true` and converting the `result` object into a proper step-like object with `districtGroups` set to `finalDistricts`, ensuring that `moveIsolatedTracts()` and other functions that expect `currentStep.districtGroups` work correctly in the final step. 

## 2154
- after step 3, then detecting isolated tracts (2 found), click move isolated and tracts are moved.
then run step 4 and isolated tracts are back as if the step is not updated after move. indeed, clicking detect isolated tracts finds the same two tracts that were moved in step 3.

**FIXED:**
1. **Cache persistence issue**: When isolated tracts (or bridge tracts) were moved in step 3, the changes were only applied to the frontend's `currentStep.districtGroups`, but not persisted to the backend algorithm state or cached step. When step 4 was requested, it used the cached step 3 (without the moves) to calculate step 4, causing the moved tracts to reappear.

2. **Update algorithm state and invalidate cache**: Fixed by:
   - Updating frontend to send `state`, `step`, and `maxIterations` when calling `moveIsolatedTractsToOppositeGroup()` and `moveBridgeTractsAndRecheck()`
   - Updating backend endpoints to:
     - Update `algorithmState.currentGroups` with the moved groups
     - Update the step in `algorithmState.steps` array if it exists
     - Invalidate the cached step for the current step number
     - Invalidate all subsequent step caches (since they depend on the current step)
   
   This ensures that when step 4 is calculated, it uses the updated step 3 (with the moves), not the original cached step 3.

   ## 2209
   - step 4 after clicking detect bridge tracts (which correctly identifies and highlights  District 5 Tract ID: 010102 as a brdige to join tracts in DG4), then clicking move bridge tracts does not move 010102.

**FIXED:**
1. **Wrong target group for bridge tract movement**: Bridge tracts are detected in the sibling group (tracts in the sibling group that are adjacent to isolated tracts), but the move logic was trying to move them TO the sibling group instead of TO the isolated group. This caused all bridge tracts to be skipped with "already in target group" because they were already in the sibling group.

2. **Fixed target group**: Changed `_moveBridgeTractsToGroup` to move bridge tracts FROM the sibling group TO the isolated group (changed `targetGroupIndex` from `siblingGroupIndex` to `isolatedGroupIndex`). Bridge tracts should be moved to the isolated group to connect the isolated tracts to the main component.

## 2019
- algorithm appears to be working correctly now through all step stages. however, i have to click move isolated tracts (for each DG?). check if move isolated moves for all DGs.

**VERIFIED:**
1. **`moveIsolatedTracts()` already moves for all DGs in one click**: The function processes all groups with isolated tracts sequentially in a single operation:
   - Gets all isolated tract IDs grouped by their isolated group index
   - Gets all groups with isolated tracts, sorted by group index
   - Creates an array of observables, one for each group
   - Executes all moves sequentially using RxJS `concat()`
   
   Example from log: "Moving isolated tracts from 3 group(s): 1, 3, 4" shows it processes multiple groups in one click.

2. **Multiple clicks may be needed**: After moving isolated tracts, new isolation may be detected (because moving tracts can create new isolation or reveal existing isolation), requiring another click. This is expected behavior - each click resolves the current isolation state, and the process may need to be repeated until all isolation is resolved.

## 2032
- nope. i watched isolated tracts get moved by DG with each click. review how moving isolated tracts determines isolated tracts to move. there should a list of isolated tracts in the step saved in cache. there may be another reason only moving isolated tracts does not move isolated tracts for all DGs in step, but confirm where the move function gets the list of isolated tracts from the step.

**VERIFIED:**
1. **Source of isolated tracts list**: The `moveIsolatedTracts()` function gets the list of isolated tracts from `this.isolatedTractsData.isolatedTractsByGroup`, which is set when `detectIsolatedTracts()` is called. This data is NOT stored in the step cache - it's stored in the component's state (`this.isolatedTractsData`).

2. **Frontend DOES process all groups in one click**: The frontend code uses RxJS `concat()` to process all groups with isolated tracts sequentially in a single operation. Console logs confirm this: "Moving isolated tracts from 2 group(s): 1, 3" shows multiple groups being processed in one click.

3. **Why it appears to move one DG at a time**: The backend endpoint accepts only one `isolatedGroupIndex` at a time, so the frontend makes multiple backend calls (one per group) sequentially within a single click. The backend logs show one group per call, which can be misleading, but the frontend is correctly processing all groups.

4. **Multiple clicks may still be needed**: After processing all groups in one click, new isolation may be detected (because moving tracts can create new isolation), requiring another click. This is expected behavior.

**NOTE**: Isolated tracts data is not currently stored in the step cache. It's only stored in component state after `detectIsolatedTracts()` is called. If you want isolated tracts to persist in the step cache, that would require adding it to the step data structure and normalization/reconstruction logic.

## 2046
- just reran the steps for AZ. problem remains as I have described. multiple move isolated clicks are required to move final isolated tracts. see logs.

**FIXED:**
1. **Observables created upfront with stale data**: The frontend was creating all observables upfront with the initial isolated tract IDs from `isolatedTractsByGroup`. After each move, the backend returns updated isolation data, but the subsequent observables were still using the initial isolated tract IDs, causing them to process stale data.

2. **Recursive processing with dynamic isolation state**: Changed `moveIsolatedTracts()` to use a recursive approach (`processNextGroup()`) that:
   - Processes groups one at a time sequentially
   - After each move, updates `this.isolatedTractsData` with the latest isolation result from the backend
   - After processing all initial groups, re-detects isolation to check if new isolation was created
   - If new isolation is found, processes those groups recursively
   - Continues until no more isolated tracts remain
   
   This ensures that each move uses the current isolation state, not the initial state, and automatically handles new isolation that may be created after each move. Now a single click will process all isolated tracts, including any new isolation created during the process, until all isolation is resolved.

   ## 2054
   - i still needed to click through the move isolated tracts 3 times in step 4 after moving bridge tracts. see logs of my testing which including going to previous step 3, then next to step 4, detect isolated, which confirmed not all isolated tracts are moved across DGs.

**FIXED:**
1. **Processing stale group list**: After moving isolated tracts from one group, the function continued processing the remaining groups from the original list, rather than re-checking which groups currently have isolated tracts. This meant that:
   - If a group no longer had isolated tracts after a previous move, it would still be processed (and skipped)
   - If new groups gained isolated tracts after a move, they wouldn't be processed until the next click

2. **Dynamic group list updates**: Fixed by:
   - After each move, checking the updated isolation result to get the current list of groups with isolated tracts
   - Processing the updated list instead of continuing with the original list
   - When skipping a group (because it no longer has isolated tracts), re-detecting isolation to get the current state and updated group list
   
   This ensures that a single click processes ALL groups that currently have isolated tracts, including any new groups that gained isolated tracts after previous moves, until all isolation is resolved.

   ## 2103
   - see logs, same problem.

   ## 2106
   - review the move isolated tracts function and create a md describing how it works. include source of list of isolated tracts for step and confirm that moving is just swapping DG of isolated tract to it's sibling_DG.

**COMPLETED:**
1. **Documentation created**: Created `doc/MOVE_ISOLATED_TRACTS_FUNCTION.md` with comprehensive documentation of the `moveIsolatedTracts()` function.

2. **Key findings documented**:
   - **Source of isolated tracts list**: Isolated tracts data is NOT stored in step cache. It's stored in frontend component state (`this.isolatedTractsData`) after `detectIsolatedTracts()` is called. The data is ephemeral and exists only in component state.
   
   - **DG swap confirmed**: Yes, moving an isolated tract is essentially swapping its `tract_DG` with its `sibling_DG`. The process:
     - Reads `sibling_DG` from isolated tract properties
     - Finds the district group matching the `sibling_DG`
     - Moves the tract from source group to target group
     - Swaps: `tract_DG` ↔ `sibling_DG` (e.g., `tract_DG="DG6-7"` becomes `tract_DG="DG8-9"` and `sibling_DG="DG8-9"` becomes `sibling_DG="DG6-7"`)

3. **Documentation includes**:
   - Complete function flow (frontend and backend)
   - Data persistence and cache invalidation
   - Recursive processing logic
   - Example flow with actual tract IDs
   - Key points summary

## 2319
- change move isolated logic flow be all on the backend. this requires persisting isolated tracts in step cache. also, moving isolated tracts should get the list of isolated tracts for the step and quickly swaps tracts to sibling_DG. this should fix current issue with FE needing multiple clicks to move tracts.

**FIXED:**
1. **Persisted isolated tracts in step cache**: Modified `createStep()` to automatically detect and store isolated tracts data when creating steps. The isolated tracts data is now part of the step structure and is persisted in the step cache via `normalizeStepData()`.

2. **New backend-only endpoint**: Created `POST /api/algorithm/move-all-isolated-tracts` that:
   - Gets isolated tracts data from step cache (or detects if not present for backward compatibility)
   - Processes ALL groups with isolated tracts in one operation
   - Swaps tracts to their `sibling_DG` (swaps `tract_DG` ↔ `sibling_DG`)
   - Updates algorithm state and invalidates step cache
   - Returns updated district groups and new isolation result

3. **Simplified frontend**: `moveIsolatedTracts()` now just calls the new backend endpoint - all recursive processing logic moved to backend. Frontend is now a simple wrapper that:
   - Checks if step has isolated tracts data (from cache) or component has manually detected data
   - Calls backend endpoint
   - Updates component state with result

4. **Automatic detection on step creation**: Steps now automatically include isolated tracts data when created, so it's always available in the step cache without requiring manual detection.

5. **Backward compatibility**: If a step doesn't have isolated tracts data (old cached steps), the backend endpoint will detect it on-the-fly.

This fixes the multiple-click issue because all processing happens in one backend operation, and the backend has access to the complete algorithm state, so it can process all groups efficiently without the frontend/backend round-trip delays.

## 2137
- see logs. same problem.

**FIXED:**
The backend endpoint was processing all groups once but not recursively handling newly created isolated tracts. After moving tracts from one group, new isolated tracts could be created in other groups, requiring multiple calls.

**Solution:**
Updated `/api/algorithm/move-all-isolated-tracts` to recursively process isolated tracts until none remain:
- Processes all groups with isolated tracts in each iteration
- Re-detects isolation after each iteration
- Continues until no isolated tracts remain or max iterations reached
- All processing happens in a single backend call

This ensures all isolated tracts are moved in one click, even when new isolation is created during the process.

## 2140
- see logs.

# 251126

## 0038
the backend now supports running the entire algorithm with endpoint POST to /api/algorithm/execute and UI has button to execute entire algorithm, but only shows the button while step 0 is loading. instead, the Run All Steps button should be a text button and visible for each step, i.e. always.