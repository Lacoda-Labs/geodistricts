# 251203

## 19:11
on maps page
- prev button still has issues after loading maps using Run All Steps (RAS). perhaps RAS is not saving/caching step progress. 
- add a "Start" text link that clears the cache and reloads the map at step 0.


## 19:39
- start does not work as expected. it should invalidate cache, instead it is just loading the final step.

## 19:58
- start button now starts at step 0, but looks like subsequent steps are still using cached steps. best thing to do is marked the cache as invalidated either using a TTL or some other approach like just deleting all cache step entries for a given state.

## 21:21
- AZ has a problem detecting bridge tracts. Step 4 should identify District 5
Tract ID: 010102 as a bridge tract, which when swapped, fixes the large isolation of District 4 tracts. when stepping from step 3 to step 4, clicking detect bridge tracts does identify 010102, but also may other tracts which are not really bridge tracts because if they flip to the sibling_DG they don't help connect isolated siblings. Further, when clicking move brige tracts that should just swap tract_DG with sibling_DG for each identified bridge tract, that is not what happens. District 5 Tract ID: 941300 is also marked as a bridge but it is not.

## 21:39
update maps page to highlight a single geodistrict (DG) when clicked on in .district-groups-table
- change all other DGs color to a grayscale of current DG color.
- hide divider lines

### 21:47
- when no district is selected do not grayscale any DGs.